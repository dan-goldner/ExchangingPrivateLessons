rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      // קריאה מותרת לכולם, כתיבה רק לבעל הקובץ
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
